const UserCard = ({user}) => (
    <div>
        
            <div>
                <h1>
                    {user.nome}
                </h1>
                
            </div>
        </div>
)

export default UserCard;